package com.example.logintypes

import android.app.Application

class App : Application() {
    //val database by lazy AppDatabase.getDatabase(this)
    //val repository by lazy UserRepository(database.LoginssDao())
}